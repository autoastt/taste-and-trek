import { Typography } from '@material-tailwind/react'

const Instruction = () => {
  return (
    <div>
      <Typography variant = "h1" className = "text-indigo-500">
        Don't you know where to go today?
      </Typography>
      <Typography variant = "Lead" color = "black">
        
      </Typography>
    </div>
  )
}

export default Instruction
